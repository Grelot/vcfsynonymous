language: python
python:
  - 3.6
  - 3.7
  - 3.8
before_install:
  - python3 --version
  - pip3 install -U pip
  - pip3 install -U pytest
  - pip3 install codecov
install:
  - pip install -r requirements.txt  
script: pytest # run tests
